UtilizadorDAO

mysql -u root -p - fazer login com password
CREATE USER 'user'@'localhost' IDENTIFIED BY '1234';
GRANT ALL PRIVILEGES ON * . * TO 'user'@'localhost';
FLUSH PRIVILEGES;

select host, user from mysql.user;- ver se encontra 'user'

sair do mysql
mysql -u user -p
CREATE DATABASE DSS;

SHOW DATABASES;

utilizador
prateleiras
palete
robo
codigos 

Pedido de descarga passa a pertencer ao PedidoFacade
